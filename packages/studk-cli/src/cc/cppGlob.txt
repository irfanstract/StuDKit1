







import {
  execSync ,
} from "node:child_process" ;

import { createRequire, } from "node:module";

import {

  /* PLATFORM PATHS */

  pathToFileURL,
  pathFromFileURL,
  fileURLToPath,
  fileURLFromPath,
  Path,
  Glb,

  /* PLATFORM I/O */
  
  IO,

  /* PLATFORM SHELL */
  
  execAsync,
  exec,
  execSync,
  spawnSync,
} from 'studk-fwcore-setups/src/util-all.mjs' ;







const cppFiles = searchGlobSync(dir, "**/*.cc") ;









