{
  "compilerOptions": {
    "allowJs": true,
    "target": "es2015",
    "jsx": "react",
    "noEmit": true,
    "strict": true,
    // Global type definitions.
    "typeRoots": ["./typings", "../node_modules/@types", "../../../../node_modules/@types" /* 3, not 2, since our branch adds prefix `@<our-company>` (hence `@studiokit/ts-node`) */ ],
  }
}
